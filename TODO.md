# TODO



More sophisticated R2 WASM loading - e.g., version management, caching?

A unified loader that fetches WASM from R2 in all runtimes?

Different storage abstraction - e.g., support for local filesystem fallback?

Local FS caching when running out side Cloudflare ?

NATS Notifications off R2.

add https://github.com/ajstarks/deck/tree/master/cmd/pngdeck as an outut

add https://github.com/ajstarks/deck/tree/master/cmd/pdfdeck as an output

Fonts might not exist. We need a way to get missing fonts from google fonts, and cache on R2, and then use these for processing.


